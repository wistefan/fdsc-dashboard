# Pinned versions (git tags or branches) for upstream OpenAPI specifications.
# Update these values to pull newer spec versions, then run:
#   npm run download:specs && npm run generate:api

TIL_VERSION="main"
TIR_VERSION="main"
CCS_VERSION="main"
ODRL_VERSION="main"
