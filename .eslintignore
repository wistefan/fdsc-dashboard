dist
node_modules
src/api/generated
# Runtime auth configuration is served dynamically by the BFF server
# (see server/src/runtime-config.ts). Exclude the generated JS file
# that may be present in public/ during local development.
public/config.js
