/**
 * Pinia store initialization module.
 *
 * Pinia is installed as a plugin in `main.ts`.
 * Individual feature stores (TIL, CCS, Policies) are defined in
 * separate files and imported where needed.
 */
export {}
